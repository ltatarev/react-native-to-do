import nextUserId from './nextUserId';
import nextListId from './nextListId';
import nextTodoId from './nextTodoId';

const service = {
  nextUserId,
  nextListId,
  nextTodoId,
};

export default service;
