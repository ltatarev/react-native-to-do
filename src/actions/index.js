import { addList, setCurrentList } from './addList';
import { addUser, login, logOut } from './addUser';
import { addTodo, toggleTodo } from './addTodo';

const actions = {
  addList,
  setCurrentList,
  addUser,
  login,
  logOut,
  addTodo,
  toggleTodo,
};

export default actions;
